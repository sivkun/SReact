import ReactElement from './ReactElement';
import ReactComponent from './ReactBaseClass';

const React = {
  createElement: ReactElement.createElement,
  Component: ReactComponent,
};

export default React;
